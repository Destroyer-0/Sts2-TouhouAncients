# 翻译待沟通事项

本文件用于记录需要与翻译（PR 提交者 / 翻译协作者）沟通确认的本地化问题。
每次遇到需要对方确认、或需要对方补充/修正的译文时，追加到本文件对应小节。

---

## 一、待确认的译名长度与空间问题

### 1. `TOUHOUANCIENTS-LUCKY_RABBIT.title` / `TOUHOUANCIENTS-LUCKY_RABBIT_POWER.title`

- 当前译文：`Bare White Rabbit of Good Fortune`
- 中文原文：幸运白兔
- 疑问：**这个译名是否过长？** 卡牌标题栏与 Power 名称栏的可用宽度有限，`Bare White Rabbit of Good Fortune`（32 个字符）显著长于原译名 `Lucky Rabbit`（12 个字符），存在被截断或换行的风险。
- 该译名取自东方 Project 官方中因幡帝的称号（幸運の素兎），语义上更贴近"白兔"的设定，但需确认显示效果。
- 待确认：是否需要简化为 `Bare White Rabbit`、`Lucky White Rabbit` 或保留全称。

---

## 二、需要修正的标点问题（已在本次 rebase 中修正，需告知翻译）

### 1. `TOUHOUANCIENTS-ALL_CREATION_HISOU.description`

- 原译文片段：`the temperament of man　are all in the palm of my hand!`
- 问题：`man` 与 `are` 之间使用的是**全角空格**（U+3000），在游戏内会造成异常的字间距。
- 处理：已在 rebase 冲突解决时改为普通半角空格。
- 需告知翻译：后续提交请避免在全角输入法状态下输入英文空格。

### 2. `TOUHOUANCIENTS-YUAN_CHOU_POWER.smartDescription`

- 原译文片段：`take　[blue]{Amount}[/blue] damage`
- 问题：`take` 与 `[blue]` 之间使用的是**全角空格**（U+3000）。
- 处理：已在 rebase 冲突解决时改为普通半角空格。

### 3. `TOUHOUANCIENTS-BLOOD_POND.description`

- 原译文：`...[/red][/red]`
- 问题：`[red]` 闭合标签重复书写了两次。
- 处理：已在 rebase 冲突解决时改为单个 `[/red]`。

### 4. `TOUHOUANCIENTS-GREED.title`

- 原译文：`"TOUHOUANCIENTS-GREED.title": "Greed"` 行末**缺少逗号**，导致整个 `enchantments.json` 解析失败。
- 处理：已在 rebase 冲突解决时补上逗号。
- 需告知翻译：该修复对应提交 `1130bd7`（Update enchantments.json）。

---

## 三、需要翻译补充的内容

### 1. `TOUHOUANCIENTS-THE_FEAST.description`

- PR 侧译文：`[gold]Exhaust[/gold] {MinExhaust}~{MaxExhaust} non-[gold]Frenzied Feast[/gold] cards from your [gold]Draw Pile[/gold]. For each card Exhausted, gain {Strength:diff()} [gold]Strength[/gold] and {Energy:energyIcons()}.\nAt the end of your turn, if this card is in your [gold]Hand[/gold], add {Copies} copies of this card to your [gold]Discard Pile[/gold].`
- 中文原文最后一行：`\n[jitter][i]全都化作我的食粮吧！！[/i][/jitter]`
- 问题：**PR 侧译文缺失最后一行风味文本**。
- 处理：本次已保留项目侧的译文（含 `[jitter][i]All shall become my food!![/i][/jitter]`），标题采用 PR 的 `Frenzied Feast`。
- 需告知翻译：请补上该行风味文本。

---

## 四、需要统一的用词一致性

### 1. 卡牌与对应 Power 的标题一致性

本次 rebase 中，卡牌标题（`cards.json`）与其对应状态标题（`powers.json`）出现过不一致的情况，已统一采用 PR（翻译）侧的译名：

| 本地化键 | 统一后译名 | 中文原文 |
| --- | --- | --- |
| `LUCKY_RABBIT` / `LUCKY_RABBIT_POWER` | `Bare White Rabbit of Good Fortune` | 幸运白兔 |
| `ALL_CREATION_HISOU` / `ALL_CREATION_HISOU_POWER` | `Heaven of Scarlet Perception for All Humankind` | 全人类的绯想天 |
| `LIFE_MUST_PERISH` / `LIFE_MUST_PERISH_POWER` | `All Life Must Perish` | 生者必灭之理 |
| `THE_KOISHI_EYE` | `The Closed Eye` | 禁闭之眼 |
| `KILLING_AURA` | `Killing Aura` | 杀戮灵气 |
| `THE_FEAST` | `Frenzied Feast` | 狂飨 |

需告知翻译：后续若再改动这些名称，请同时更新 `cards.json` 与 `powers.json` 两处。

### 2. `TOUHOUANCIENTS-PURE_HELL_OF_BULLETS.description`

- 当前译文（项目侧）：`Choose any number of cards from your [gold]Hand[/gold] and [gold]Transform[/gold] them into [gold]Nameless Bullets[/gold].`
- 对应卡牌名：`TOUHOUANCIENTS-NAMELESS_BULLETS.title` = `Nameless Danmaku`
- 中文原文：`变化为[gold]无名的弹幕[/gold]`
- 问题：描述中写作 `Nameless Bullets`，但卡牌名为 `Nameless Danmaku`，**同一张牌出现两种译名**。
- 待确认：应统一为 `Nameless Danmaku` 还是 `Nameless Bullets`。

---

## 五、需要翻译确认的机制相关译文（PR 使用了旧机制文本）

以下条目的 PR 译文中描述的机制与当前中文原文不符（PR 基于较旧版本），本次已保留项目侧译文。
需告知翻译：如果后续 PR 仍基于旧版本，请先同步最新的 `zhs/` 文件再翻译。

| 本地化键 | PR 描述机制 | 当前中文机制 |
| --- | --- | --- |
| `TOUHOUANCIENTS-MAGIC_WALLET` | `Your next {CurrentFreeCount} other cards are free to play…` | 将 `{Cards}` 张随机**无色牌**添加到你的手牌，这些牌在本回合可以免费打出。这张牌的耗能增加 `1`，生成的无色牌数量增加 `1`。 |
| `TOUHOUANCIENTS-RICHEST_FORM` | `Gain {StartingCapital:diff()} Funds…`（启动资金 / Funds 机制） | 获得 `{Energy2}`。在你的回合开始时，获得 `{Energy3}`。你每打出一张牌，同类型牌在本回合耗能增加 `{Energy}`。 |
| `TOUHOUANCIENTS-POOREST_FORM` | `…apply one of the following at random to a random enemy…` | 在你的回合结束时，手牌总耗能每比剩余能量多一，就给予随机一名敌人 `{Amount}` 层灾厄 `1` 次。 |
| `TOUHOUANCIENTS-RICHEST_FORM_POWER` / `TOUHOUANCIENTS-POOREST_FORM_POWER` | `Opulent Form` / `Destitute Form`（Funds 机制） | 同上的新机制 |
| `TOUHOUANCIENTS-CAPITAL_POWER` | `Funds`（启动资金） | **该 Power 已被删除，中文侧无对应条目** |

---

## 六、PR 第二轮（日语翻译）中发现的问题

PR 的第二轮提交（`ab27345`、`b394003`、`fcc43ec`、`788fe8c`、`8e9da53`、`d622b28`、`2f111b9`、`b8542cd`）为 `jpn/` 目录写入了真正的日语翻译。
以下为该轮中日语文本与当前中文原文不一致、或日语自身存在错误的地方，均已在 2026-09-10 的 rebase 中处理，需要告知翻译。

### 1. 与当前中文机制不符的日语文本（已改用项目侧英文）

| 本地化键 | PR 日语描述的是 | 当前中文实际是 |
| --- | --- | --- |
| `TOUHOUANCIENTS-MAGIC_WALLET` | 免费打出接下来 N 张牌（旧机制） | 将 `{Cards}` 张随机**无色牌**添加到你的手牌，这些牌在本回合可以免费打出。这张牌的耗能增加 `1`，生成的无色牌数量增加 `1`。 |
| `TOUHOUANCIENTS-RICHEST_FORM` | 资金（Funds）机制 | 获得 `{Energy2}`。在你的回合开始时，获得 `{Energy3}`。你每打出一张牌，同类型牌在本回合耗能增加 `{Energy}`。 |
| `TOUHOUANCIENTS-POOREST_FORM` | 打出 0 费牌时给随机敌人随机 debuff | 在你的回合结束时，手牌总耗能每比剩余能量多一，就给予随机一名敌人 `{Amount}` 层灾厄 `1` 次。 |
| `TOUHOUANCIENTS-LILY_BELL` | 自身中毒累积的旧版效果 | 获得 `{Energy}`，抽 `{Cards}` 张牌。给予所有敌人等同于自身 `[purple]污染[/purple]` 层数 `{Multiplier:diff()}` 倍的 `[gold]中毒[/gold]`。 |
| `TOUHOUANCIENTS-CAPITAL_POWER` | `資金`（启动资金） | **该 Power 已被删除，中文侧无对应条目** |

处理方式：这些键的**标题保留日语**，**描述保留项目侧英文**，待翻译同步最新 `zhs/` 后重新翻译。

### 2. `TOUHOUANCIENTS-GEISHEHUAXIAOJIEJIANSHAOLILIANG_POWER`（给蛇花小姐减少8点力量）

同一段文本在中 / 英 / 日三处互不相同：

| 位置 | 标题 | 风味文本 |
| --- | --- | --- |
| 中文（`zhs/powers.json`） | 给蛇花小姐减少8点力量 | 觉得自己还可以战斗而不跑，殊不知自己已经虚弱到一点伤害也没有了。 |
| 英文（`eng/powers.json`） | `My Little Snake Plant's Strength Can't Be This Low!` | `Bro thinks he deals any damage.` |
| 日语（PR，已采用） | `うちのスネークプラントちゃんの筋力がこんなに低いわけがない` | `貧弱！貧弱ゥ！` |

待确认：以哪一处为准？中文侧是否需要用日文/英文的玩梗译法，或反过来统一为中文原意。

### 3. `TOUHOUANCIENTS-LIFE_MUST_PERISH_POWER` 触发条件三处不一致

| 位置 | 触发条件 |
| --- | --- |
| 中文（`zhs`） | 受到**敌人伤害**时 |
| 英文（`eng`） | `When you receive **unblocked** damage`（受到**未被格挡**的伤害时） |
| 日语（PR） | `敵から**攻撃ダメージ**を受けるたび`（受到来自敌人的**攻击伤害**时） |

待确认：实际游戏逻辑到底是哪一种（敌人任意伤害 / 未被格挡的伤害 / 敌人的攻击伤害），三处应统一。

### 4. `TOUHOUANCIENTS-YUAN_CHOU_POWER` 标题用词不一致

| 位置 | 标题 |
| --- | --- |
| 中文 | 怨仇 |
| 英文 | `Grudge` |
| 日语（已采用 PR） | 怨念 |

待确认：日语的「怨念」是否可接受（与中文「怨仇」用字不同，但语义接近；同时与描述内部的 `[gold]怨念[/gold]` 标签保持一致）。

### 5. `TOUHOUANCIENTS-ALL_CREATION_HISOU` 日语误译（已修正）

- 原日语：`[gold]手札[/gold]と[gold]捨て札[/gold]のカードをすべてプレイし…`
- 问题：中文是"手牌与**消耗牌堆**"，日语写成了"手牌与**弃牌堆**"（捨て札）。
- 处理：已按基础游戏的既有用词改为 `[gold]手札[/gold]と[gold]廃棄札[/gold]`（`廃棄札` 见基础游戏 `cards.json` 的 `ASHEN_STRIKE.description`）。
- 需告知翻译：`Exhaust Pile` 在日文中的既定译法是 **`廃棄札`**，不是 `捨て札`。

#### 5.1 `{IfUpgraded:show:...}` 中的 `[gold]` 标记已移除（三语不一致）

- 处理：按项目方要求，日语与英语的 `{IfUpgraded:show:...}` 分支均**去掉 `[gold]` 标记**。
  - 英语（`eng/cards.json`）：`{IfUpgraded:show:Upgrade, |}`
  - 日语（`jpn/cards.json`）：`{IfUpgraded:show:アップグレードし, |}`
- 遗留问题：中文（`zhs/cards.json`）仍是带标记的写法：
  - `{IfUpgraded:show:[gold]升级[/gold]、|}`
- 需告知翻译 / 待确认：中文侧是否也要去掉 `[gold]` 以保持三语一致？若中文保留标记，则同一张卡在三种语言下的"升级"二字高亮表现会不同。

### 6. `TOUHOUANCIENTS-SUPER_ZUN_BEER_POTION` 日语笔误（已修正）

- 原日语：`ンダムな東方先古の民カード1枚を[gold]手札[/gold]に加える。`
- 问题：开头**漏掉了「ラ」**，应为 `ランダムな…`。
- 处理：已在 rebase 中补上。

### 7. `TOUHOUANCIENTS-SHI_RA_NAI_POTION`（希腊奶）的标题与风味文本

#### 7.1 标题

| 位置 | 标题 |
| --- | --- |
| 中文 | 希腊奶（"不知道"的谐音） |
| 英文原译（项目侧） | `Shi Ra Nai Milk`（直译"希腊奶"） |
| PR 侧（本次采用） | `WakaraNo idea` |

- 说明：`WakaraNo idea` 是日语 `わからない`（不知道）与英语 `No idea` 的混合谐音，与中文"希腊奶"的双关结构对应，故本次改用 PR 侧。

#### 7.2 风味文本

| 位置 | 风味文本 |
| --- | --- |
| 中文 | 想不出这个药水的风味文本了，也许只能这样了呢（笑）。 |
| 英文原译（项目侧） | `Couldn’t think of a flavor text for this potion, so this will have to do (lol).` |
| PR 侧（本次采用） | `The pun died during translation. Pretend this being a potion called Bone Apple Tea that gives you 3 copies of Ironclad's Feed.` |

- 问题 1：PR 侧完全脱离了中文原意，改成了一个关于"翻译不好双关"的元笑话，并引用了 `Bone Apple Tea`、`Ironclad's Feed` 等与原药水无关的内容。
- 问题 2：语法有误，`Pretend this being a potion` 应为 `Pretend this is a potion` 之类的正确写法。
- 待确认：是否保留 PR 侧的风味文本？若保留，建议先修正语法。

#### 7.3 描述缺少第二行（英日均有此问题）

- 中文原文：`将一张消耗堆的牌加入到手牌。\n想不出这个药水的效果了，也许只能这样了呢（笑）`
- 英文（PR 侧，本次采用）：`Put a card from your [gold]Exhaust Pile[/gold] into your [gold]Hand[/gold].`（**只有第一行**）
- 日语（PR 侧）：`廃棄されたカード1枚を[gold]手札[/gold]に加える。`（**只有第一行**）
- 需告知翻译：英、日两侧均缺少第二行趣味文本，请补充。

### 8. `TOUHOUANCIENTS-THE_FEAST` 日语描述缺少风味行

- 中文原文最后一行：`\n[jitter][i]全都化作我的食粮吧！！[/i][/jitter]`
- 日语（PR）：无此行。
- 处理：本次采用项目侧英文描述（含 `[jitter][i]All shall become my food!![/i][/jitter]`），标题采用日文 `狂宴`。
- 需告知翻译：请补上该行风味文本的日文。

### 9. 中日文本未同步导致的重复键风险

`d622b28`（Update cards.json）中，日语的 `jpn/cards.json` 只有 45 个键，而项目侧有 78 个键；且键的顺序与项目侧不同，导致 git 合并时产生大量重复键。
本次 rebase 已以项目侧的键结构为基准重建该文件。
需告知翻译：修改 `jpn/cards.json` 前请先与最新的 `zhs/cards.json` 对齐键集合与顺序，避免再次出现重复或缺键。

### 10. `TOUHOUANCIENTS-TOUHOUANCIENTKOISHIUNPLAYABLE` 描述已改为 PR 版本

- 项目侧：`Cannot be played without [purple]Auto Play[/purple].`（与中文"无法以[purple]自动打出[/purple]以外的方式打出。"一致）
- PR 侧（本次采用）：`Cannot be played from hand. Can only be played by other cards or effects.` / 日语 `手札からはプレイ不可。他のカードや効果でのみプレイされる。`
- 需确认：该改法不再引用 `Auto Play` 关键字，且与基础游戏 `UNPLAYABLE` 的 "cannot be played from **anywhere**" 用词不一致。

